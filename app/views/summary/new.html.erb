 <h1>New <%= @post.title %> Summary</h1>
 
 <div class="row">
   <div class="col-md-4">
     <p>Guidelines for Summaries:</p>
     <ul>
       <li>Make sure the summary is appropriate.</li>
       <li>Include roughly 3-5 sentences.</li>
       <li>Use original content.</li>
     </ul>
   </div>
   <div class="col-md-8">
     <%= form_for new_topic_post_summary_path do |f| %>
       <div class="form-group">
         <%= f.label :description %>
         <%= f.text_area :description, rows: 8, class: 'form-control', placeholder: "Enter description here" %>
       </div>
       <%= f.submit "Save Summary", class: 'btn btn-success' %>
     <% end %>
   </div>
 </div>
